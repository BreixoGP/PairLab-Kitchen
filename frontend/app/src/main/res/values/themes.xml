<resources>

    <!-- THEME NORMAL DE LA APP (HOME, FRAGMENTS, ETC) -->
    <style name="Theme.FPappfront" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
    <style name="Theme.FPappfront.PostSplash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- SPLASH SOLO PARA API < 31 -->
    <style name="Theme.FPappfront.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

</resources>